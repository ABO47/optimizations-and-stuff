# 0.1.0-alpha

- Server-side performance toggles with per-flag benchmarking (`/opt bench`, `/opt test`, `/opt flag`)
- View distance clamps and dormancy, memory guard with entity cap, far-view entity decoupling
- Item merging, instant tree felling, chunk streaming budgets, GC stutter guard
